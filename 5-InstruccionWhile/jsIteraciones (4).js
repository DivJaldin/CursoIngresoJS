function Mostrar()
{

	var Numero = prompt("ingrese un número entre 0 y 10.");
while (Numero<0 || Numero >9)
	{
		console.log (Numero);
		if (Numero == null);
			{alert ("falso")};
	}

}//FIN DE LA FUNCIÓN